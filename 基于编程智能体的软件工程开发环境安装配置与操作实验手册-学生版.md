# 编程智能体辅助下的软件工程开发环境安装、配置与操作实验手册

## 一、手册定位

本手册用于指导《软件工程》课程实验环节中，学生如何配置并使用基于编程智能体的开发环境。

本手册所说的“编程智能体”包括但不限于：

- Codex App。
- GitHub Copilot。
- Cursor。
- Claude Code。
- 通义灵码。
- 百度 Comate。
- 其他可读取代码、生成代码、辅助测试和进行代码审查的 AI Coding 工具。

本课程推荐是：

```text
Git 仓库作为项目中心
-> 需求和架构约束写入仓库文档
-> Issue 或任务卡片描述开发任务
-> 编程智能体基于仓库、任务和约束生成代码变更
-> 学生审查 diff
-> 本地 IDE 运行、调试和测试
-> commit 或 PR 留痕
-> 提交过程证据
```

核心原则：

> 编程智能体可以生成代码，但需求确认、架构约束、代码审查、测试验证和工程责任必须由学生完成。

## 二、推荐开发环境总体结构

## 1. 各工具的关系

### 直接连接 GitHub 的模式

如果智能体或其他编程智能体支持直接连接 GitHub 仓库，则推荐采用：

```text
智能体 <-> GitHub 仓库
学生本地 Git 仓库 <-> GitHub 仓库
IDE <-> 学生本地 Git 仓库
```

在这种模式下：

- GitHub 是远程项目中心。
- 智能体直接读取 GitHub 仓库、Issue、文档和代码。
- 智能体可生成分支、diff 或 PR。
- 学生在 GitHub 上审查 PR，也可以把分支拉到本地。
- IDE 用于本地运行、调试和测试。

### 本地仓库模式

如果智能体不能直接连接 GitHub，或者学生账号和网络环境不支持，则采用：

```text
GitHub/Gitee 仓库
  ↓ git clone
本地 Git 仓库
  ↓
IDE 打开项目
  ↓
编程智能体读取本地代码或学生提供的上下文
  ↓
学生审查 diff、运行测试、commit、push
```

在这种模式下：

- 学生先把远程仓库克隆到本地。
- IDE 打开本地项目。
- 编程智能体在本地项目中辅助编码，或通过复制上下文生成局部代码。
- 学生本地审查、测试、提交，再推送到远程仓库。

## 2. 课程推荐策略

考虑本科实验课中学生工具基础差异较大，建议采用分层要求：

| 层次 | 要求 | 适合对象 |
| --- | --- | --- |
| 基础要求 | Git + IDE + 任意 AI Coding 工具 + 本地测试 | 全体学生 |
| 推荐要求 | GitHub/Gitee Issue + 分支 + PR 或代码审查记录 | 大多数小组 |
| 提高要求 | 智能体直接连接 GitHub，基于 Issue 生成 PR | 基础较好的小组或助教演示 |

课程评分不以使用某个特定工具为标准，而以过程证据为标准：

- 是否有清晰任务。
- 是否有需求和架构依据。
- 是否有 AI 使用记录。
- 是否有 diff 审查。
- 是否有测试结果。
- 是否有 commit 或 PR 记录。

## 三、本地环境安装清单

## 1. 必装工具

每名学生电脑建议安装：

| 工具 | 作用 |
| --- | --- |
| Git | 版本管理、查看 diff、提交 commit、拉取分支 |
| IDE | 编写、运行和调试代码 |
| 浏览器 | 访问 GitHub/Gitee、查看 Issue 和 PR |
| AI Coding 工具 | 辅助需求、架构、编码、测试和代码审查 |
| 项目运行环境 | 根据项目技术栈安装 Node.js、JDK、Python 等 |

## 2. IDE 选择

可选择：

- Visual Studio Code。
- IntelliJ IDEA。
- WebStorm。
- PyCharm。
- Cursor。
- 其他支持 Git 的开发工具。

建议：

- 前端、Node.js、Python 项目优先使用 VS Code 或 Cursor。
- Java/Spring Boot 项目优先使用 IntelliJ IDEA。

## 3. 技术栈环境

### Node.js 项目

需要安装：

- Node.js。
- npm 或 pnpm。

常用命令：

```bash
node -v
npm -v
npm install
npm test
npm run dev
```

### Java/Spring Boot 项目

需要安装：

- JDK 17 或 JDK 21。
- Maven 或 Gradle。
- IntelliJ IDEA。

常用命令：

```bash
java -version
mvn -v
mvn test
mvn spring-boot:run
```

### Python/Flask 或 Django 项目

需要安装：

- Python 3.10 或以上版本。
- pip。
- 虚拟环境工具。

常用命令：

```bash
python --version
pip install -r requirements.txt
pytest
python app.py
```

## 四、Git 与 GitHub 基础配置

## 1. Git 基本概念

| 概念 | 含义 |
| --- | --- |
| 仓库 Repository | 一个项目的代码和历史记录 |
| 本地仓库 | 学生电脑上的项目副本 |
| 远程仓库 | GitHub/Gitee/学校 GitLab 上的项目仓库 |
| commit | 一次代码变更记录 |
| branch | 分支，用于隔离开发任务 |
| diff | 本次修改前后的差异 |
| push | 把本地提交上传到远程仓库 |
| pull | 从远程仓库拉取最新代码 |
| PR | Pull Request，请求审查并合并代码 |
| Issue | 任务卡片或问题单 |

## 2. 配置 Git 用户信息

首次使用 Git 时配置：

```bash
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
```

查看配置：

```bash
git config --global --list
```

## 3. 克隆远程仓库

```bash
git clone https://github.com/组织名/仓库名.git
```

进入项目目录：

```bash
cd 仓库名
```

查看状态：

```bash
git status
```

## 4. 常用 Git 操作

查看修改：

```bash
git diff
```

添加修改：

```bash
git add .
```

提交修改：

```bash
git commit -m "docs: add requirements baseline"
```

推送到远程：

```bash
git push
```

拉取远程更新：

```bash
git pull
```

创建并切换分支：

```bash
git checkout -b feature/issue-003-stage-submission
```

切换分支：

```bash
git checkout main
```

## 五、项目仓库推荐结构

每个小组仓库建议采用如下结构：

```text
se-course-team-03/
  AGENTS.md
  README.md
  docs/
    requirements.md
    architecture.md
    api.md
    test-plan.md
    ai-usage-log.md
    issues/
      ISSUE-001-submit-stage-deliverable.md
    decisions/
      ADR-001-architecture-style.md
  src/
  tests/
  .gitignore
```

## 1. README.md

用于说明：

- 项目名称。
- 小组成员。
- 技术栈。
- 运行方式。
- 测试方式。
- 仓库结构。

## 2. docs/requirements.md

保存确认后的需求基线，包括：

- 项目背景。
- 系统边界。
- 用户角色。
- 用户故事。
- 核心用例。
- 业务规则。
- 权限矩阵。
- 状态规则。
- 需求有效性验证。
- 需求基线。

## 3. docs/architecture.md

保存架构设计，包括：

- 架构目标。
- 架构关注点。
- 候选架构比较。
- 最终架构选择。
- 模块划分。
- 分层规则。
- 数据模型。
- 权限与安全设计。
- 状态流转设计。
- 接口与错误处理规范。
- 可测试性设计。
- AI Coding 架构约束。

## 4. docs/api.md

保存接口文档，包括：

- API 路径。
- 请求方法。
- 请求参数。
- 响应格式。
- 错误码。
- 权限要求。

## 5. docs/test-plan.md

保存测试计划，包括：

- 测试范围。
- 测试覆盖项。
- 测试用例。
- 需求-测试追踪矩阵。
- 测试执行结果。

## 6. docs/ai-usage-log.md

保存 AI 使用记录。

格式建议：

| 日期 | 阶段 | 使用目的 | Prompt 摘要 | AI 输出 | 人工修改 | 验证方式 | 责任人 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| | | | | | | | |

## 7. AGENTS.md

用于告诉编程智能体本项目的长期规则。

示例：

```text
# AGENTS.md

## 项目规则

1. 所有代码必须遵守 docs/architecture.md。
2. Controller 只负责接收请求和返回响应，不写核心业务逻辑。
3. Service 负责业务规则、权限判断和状态流转。
4. Repository 只负责数据访问。
5. 所有接口返回统一格式：code、message、data。
6. 权限校验必须在后端完成，不能只依赖前端隐藏按钮。
7. 不允许修改与当前 Issue 无关的文件。
8. 不允许引入未经确认的新依赖。
9. AI 生成代码必须配套测试。
10. 每次修改后必须更新必要文档或测试。
```

注意：不同 AI Coding 工具对 `AGENTS.md` 的支持程度不同。即使工具不能自动读取，学生也应将其中关键约束复制到 Prompt 中。


## 六、学生项目初始化流程

## 1. 从模板创建小组仓库

学生小组创建自己的项目仓库，例如：

```text
se-course-team-03
```

## 2. 克隆到本地

```bash
git clone https://github.com/组织名/se-course-team-03.git
cd se-course-team-03
```

## 3. 用 IDE 打开项目

在 IDE 中选择：

```text
Open Folder / Open Project
```

打开 `se-course-team-03` 目录。

## 4. 修改 README.md

写入：

- 项目名称。
- 小组成员。
- 分工。
- 项目方向。
- 技术栈。

## 5. 提交初始修改

```bash
git add README.md
git commit -m "docs: add team project information"
git push
```

## 七、需求和架构文档落库流程

## 1. 在智能体中辅助讨论需求

Prompt 示例：

```text
我们要开发“...平台系统”。

请先不要生成代码。
请帮助我们做需求分析：
1. 提出需求澄清问题；
2. 识别用户角色；
3. 生成用户故事；
4. 生成核心用例；
5. 提取业务规则；
6. 建立权限矩阵；
7. 指出可能遗漏的异常流程。
```

## 2. 人工确认需求

学生必须完成：

- 删除不合理需求。
- 补充真实业务规则。
- 标记必做、扩展、暂不实现需求。
- 进行需求有效性验证。

## 3. 写入需求文档

将确认后的内容写入：

```text
docs/requirements.md
```

## 4. 在智能体中辅助讨论架构

Prompt 示例：

```text
下面是我们确认后的需求摘要。
请不要生成代码。
请帮助我们进行架构设计：
1. 从需求中提取架构关注点；
2. 给出多个候选架构方案；
3. 比较简单单体、模块化单体和微服务；
4. 推荐适合本科课程项目的方案；
5. 给出模块划分；
6. 给出核心数据模型；
7. 给出接口设计建议；
8. 给出 AI Coding 架构约束。
```

## 5. 人工确认架构

学生必须完成：

- 选择最终架构。
- 明确模块边界。
- 明确分层规则。
- 明确权限和状态规则。
- 明确接口契约。
- 明确 AI Coding 约束。

## 6. 写入架构文档

写入：

```text
docs/architecture.md
docs/api.md
docs/decisions/ADR-001-architecture-style.md
AGENTS.md
```

## 7. 提交需求和架构基线

```bash
git add docs AGENTS.md
git commit -m "docs: add requirements and architecture baseline"
git push
```

## 八、在 GitHub 中创建 Issue

## 1. 创建方式

方式一：GitHub 页面创建。

1. 打开小组仓库。
2. 点击 `Issues`。
3. 点击 `New issue`。
4. 填写任务内容。
5. 指派负责人。
6. 添加标签。
7. 保存。

方式二：用 Markdown 文件代替。

如果不熟悉 GitHub Issue，可在仓库中创建：

```text
docs/issues/ISSUE-001-submit-stage-deliverable.md
```

然后提交到 Git。

## 2. Issue 示例

```text
# Issue #3：实现阶段成果正式提交功能

## 关联需求

US-04 作为组长，我希望能够代表团队提交阶段成果，以便完成阶段交付。

## 关联验收标准

AC-04-01 只有组长可以正式提交。
AC-04-02 正式提交后必须创建 SubmissionVersion。
AC-04-03 提交状态应从草稿变为已提交。
AC-04-04 普通成员尝试正式提交时应被拒绝。

## 业务规则

BR-03 只有组长可以正式提交阶段成果。
BR-04 正式提交不能覆盖历史版本。

## 架构约束

参考 docs/architecture.md 和 AGENTS.md。

## 允许修改范围

- src/submission/
- tests/submission/

## 不允许修改范围

- 用户登录模块
- 课程项目模块
- 数据库全局配置

## 测试要求

1. 组长提交成功。
2. 普通成员提交失败。
3. 提交后生成 SubmissionVersion。
4. 重新提交不覆盖历史版本。

## 完成标准

- 代码通过测试。
- diff 中无无关文件修改。
- AI 使用记录已更新。
```

## 九、智能体直接连接 GitHub 仓库模式

## 1. 前提条件

该模式需要：

- 学生有 GitHub 账号。
- 小组仓库在 GitHub 上。
- 智能体当前账号支持 GitHub 仓库连接。
- 仓库授权成功。
- 如果是组织仓库，组织管理员允许智能体访问。

如果以上条件不满足，使用第十一节的本地仓库模式。

## 2. 连接仓库

一般操作流程：

1. 打开智能体或大模型。
2. 选择连接 GitHub。
3. 跳转到 GitHub 授权页面。
4. 选择允许访问的仓库。
5. 返回智能体。
6. 选择小组项目仓库。
7. 确认智能体可以读取仓库文件。

实际按钮名称可能因版本变化略有不同，以当前界面为准。

## 3. 基于 Issue 启动任务

在智能体中输入：

```text
请基于当前仓库处理 Issue #3：实现阶段成果正式提交功能。

开始前请先阅读：
- AGENTS.md
- docs/requirements.md
- docs/architecture.md
- docs/api.md

请先给出实现计划，不要直接修改代码。
```

## 4. 审查实现计划

检查智能体计划是否包含：

- 需要修改哪些文件。
- 是否遵守模块边界。
- 是否包含权限校验。
- 是否包含状态流转。
- 是否保存历史版本。
- 是否包含测试。

如果计划不合理，要求智能体先修改计划。

## 5. 分阶段生成代码

不要让智能体一次完成所有工作。推荐分四步：

### 第一步：数据模型和 Repository

```text
请只检查和补充 Submission、SubmissionVersion 相关数据模型和 Repository。
不要修改 Controller。
不要修改前端。
不要引入新依赖。
```

### 第二步：Service 层逻辑

```text
请只实现 SubmissionService 中的正式提交逻辑。
必须包含：
1. 组长权限校验；
2. 截止时间校验；
3. 必需交付物校验；
4. 创建 SubmissionVersion；
5. 提交状态变更。
```

### 第三步：Controller/API

```text
请基于 docs/api.md 中的接口规范，实现正式提交 API。
Controller 只负责请求接收和响应返回，不写业务逻辑。
```

### 第四步：测试

```text
请根据 Issue 中的验收标准生成测试。
至少覆盖：
1. 组长提交成功；
2. 普通成员提交失败；
3. 提交后生成 SubmissionVersion；
4. 重新提交不覆盖旧版本。
```

## 6. 智能体 生成 PR 后的处理

如果智能体创建了分支或 PR，学生需要：

1. 打开 GitHub PR 页面。
2. 查看 `Files changed`。
3. 对照 Issue 和架构约束审查 diff。
4. 如果需要本地测试，将分支拉到本地。

拉取分支示例：

```bash
git fetch origin
git checkout feature/issue-003-stage-submission
```

然后在 IDE 中运行和测试。

## 十、本地仓库模式

当智能体不能直接连接 GitHub 时，使用本地仓库模式。

## 1. 操作流程

```text
GitHub/Gitee 仓库
-> git clone 到本地
-> IDE 打开项目
-> 将 Issue、需求、架构约束和代码片段提供给智能体
-> 智能体生成局部代码
-> 学生放入项目
-> git diff 审查
-> 运行测试
-> git commit
-> git push
```

## 2. 本地模式 Prompt

```text
下面是当前任务、需求、架构约束和相关代码。
请只完成指定局部任务。

当前任务：

关联需求：

验收标准：

架构约束：

已有代码：

允许修改范围：

禁止事项：

测试要求：

请先给出实现计划，不要直接生成代码。
```

## 3. 本地模式注意事项

- 只复制局部代码。
- 必须人工阅读和适配。
- 必须通过 `git diff` 查看修改。
- 必须运行测试。
- 必须记录 AI 使用过程。

## 十一、diff 审查流程

## 1. 什么是 diff

diff 是本次修改前后代码的差异。

它能显示：

- 新增了哪些文件。
- 删除了哪些文件。
- 修改了哪些代码。
- 哪些行被加入。
- 哪些行被删除。

## 2. 查看修改文件

```bash
git status
```

检查：

- 是否只修改了 Issue 允许的文件。
- 是否改了无关模块。
- 是否生成了多余文件。
- 是否修改了全局配置。

## 3. 查看具体差异

```bash
git diff
```

也可以使用 IDE 的 Git diff 面板。

## 4. diff 审查清单

| 审查项 | 检查问题 |
| --- | --- |
| 需求符合性 | 是否满足 Issue 中的验收标准 |
| 修改范围 | 是否只改了允许修改的目录 |
| 架构遵循 | 是否遵守 Controller/Service/Repository 分层 |
| 权限安全 | 是否在后端做权限校验 |
| 状态处理 | 是否通过统一方法修改状态 |
| 数据一致性 | 是否创建版本，是否覆盖历史数据 |
| 错误处理 | 是否使用统一错误格式 |
| 测试覆盖 | 是否补充了对应测试 |

## 5. 审查记录表

| 审查项 | 结果 | 问题 | 处理 |
| --- | --- | --- | --- |
| 修改范围 | 通过/不通过 | | |
| 架构遵循 | 通过/不通过 | | |
| 权限校验 | 通过/不通过 | | |
| 状态处理 | 通过/不通过 | | |
| 测试覆盖 | 通过/不通过 | | |

## 十二、本地运行与测试

## 1. Node.js 项目

```bash
npm install
npm test
npm run dev
```

## 2. Java/Spring Boot 项目

```bash
mvn test
mvn spring-boot:run
```

## 3. Python 项目

```bash
pip install -r requirements.txt
pytest
python app.py
```

## 4. 测试失败处理

不要直接让 AI 大范围重写代码。应先记录失败信息，再让 AI 做最小修复分析。

Prompt 示例：

```text
下面是测试失败日志、相关代码和 Issue 验收标准。
请判断失败原因。
要求：
1. 判断是测试脚本错误、代码实现错误、需求理解错误还是环境问题。
2. 给出证据。
3. 给出最小修改建议。
4. 不要大范围重写代码。
```

## 十三、PR 审查与合并

## 1. PR 创建方式

如果智能体支持创建 PR，可由智能体创建。

如果基于本地开发，可手动：

1. 创建功能分支。
2. commit。
3. push 到 GitHub。
4. 在 GitHub 页面创建 PR。

## 2. PR 描述模板

```text
## 关联 Issue

## 完成内容

## AI 参与内容

## 人工修改内容

## 测试结果

## diff 审查结果

## 已知问题
```

## 3. 合并前检查

合并前必须确认：

- Issue 中验收标准已满足。
- diff 已审查。
- 测试已运行。
- 无无关文件修改。
- AI 使用记录已填写。
- 至少一名组员完成审查。

## 4. 合并

PR 审查通过后，合并到主分支。

本地同步：

```bash
git checkout main
git pull
```

## 十四、AI 使用记录要求

每次关键 AI 使用都要记录。

| 日期 | 阶段 | 使用目的 | Prompt 摘要 | AI 输出 | 人工修改 | 验证方式 | 责任人 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| | | | | | | | |

记录重点不是完整复制所有对话，而是说明：

- AI 做了什么。
- 小组采纳了什么。
- 小组修改了什么。
- 如何验证结果。

## 十五、一次完整开发任务示例

以“实现阶段成果正式提交功能”为例。

## 1. 创建 Issue

Issue 写清：

- 关联需求。
- 验收标准。
- 业务规则。
- 架构约束。
- 允许修改范围。
- 测试要求。

## 2. 智能体制定计划

Prompt：

```text
请基于当前仓库处理 Issue #3。
请先阅读 AGENTS.md、docs/requirements.md、docs/architecture.md、docs/api.md。
先给出实现计划，不要直接修改代码。
```

## 3. 分阶段生成

依次生成：

- 数据模型。
- Service。
- Controller/API。
- 测试。

## 4. 审查 diff

检查：

- 是否只修改 `src/submission/` 和 `tests/submission/`。
- 是否创建 `SubmissionVersion`。
- 是否有后端权限校验。
- 是否统一错误处理。

## 5. 运行测试

```bash
npm test
```

或：

```bash
mvn test
```

或：

```bash
pytest
```

## 6. 提交或合并 PR

测试通过后提交：

```bash
git add .
git commit -m "feat(submission): implement stage deliverable submission"
git push
```

如果使用 PR，则合并 PR。

## 7. 更新 AI 使用记录

在 `docs/ai-usage-log.md` 中记录本次 AI 使用。

## 十六、实验考核证据

每个开发任务完成后，应提交：

1. Issue 或任务卡片。
2. 关联需求和验收标准。
3. 基于AI编程工具的实现计划。
4. Prompt 摘要。
5. diff 或 PR 链接。
6. 人工审查记录。
7. 测试结果。
8. commit 或 PR 合并记录。
9. AI 使用记录。

## 十七、分组实施建议


- 每 3-4 名学生组成 1 组。
- 每组完成 2-3 个可追踪开发任务。
